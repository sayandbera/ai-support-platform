import { ConversationsIdLayout } from "@/modules/dashboard/ui/layouts/conversation-id-layout";

const Layout = ({ children }: { children: React.ReactNode }) => {
  return <ConversationsIdLayout>{children}</ConversationsIdLayout>;
};

export default Layout;
